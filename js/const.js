'use strict';

(function () {
  var ENTER_KEY = 'Enter';
  var MIN_PRICE = 0;
  var MAX_PRICE = 1000000;

  window.const = {
    ENTER_KEY: ENTER_KEY,
    MIN_PRICE: MIN_PRICE,
    MAX_PRICE: MAX_PRICE
  };
})();
