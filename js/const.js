'use strict';

(function () {
  var ENTER_KEY = 'Enter';
  var MIN_PRICE = 0;
  var MAX_PRICE = 1000000;
  var PINS_COUNT = 5;

  window.const = {
    ENTER_KEY: ENTER_KEY,
    MIN_PRICE: MIN_PRICE,
    MAX_PRICE: MAX_PRICE,
    PINS_COUNT: PINS_COUNT
  };
})();
